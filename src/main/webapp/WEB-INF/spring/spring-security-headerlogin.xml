<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:context="http://www.springframework.org/schema/context"
             xsi:schemaLocation=
                     "http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.3.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <global-method-security secured-annotations="enabled" pre-post-annotations="enabled"/>

    <http use-expressions="true" entry-point-ref="customEntryPoint" create-session="stateless">
        <intercept-url pattern="/rest/**" access="isAuthenticated()"/>
        <custom-filter position="BASIC_AUTH_FILTER" ref="authenticationFilter" />

        <!--<http-basic />--> <!-- For login via HTTP Headers -->
        <!--<csrf disabled="true"/>-->
    </http>

    <beans:bean id="customEntryPoint" class="mx.com.example.test.config.security.CustomBasicAuthenticationEntryPoint">
        <beans:property name="realmName" value="cardinalRealmName" />
    </beans:bean>

    <beans:bean id="authenticationFilter" class="mx.com.example.test.config.security.DemoAuthenticationFilter"/>

    <authentication-manager>
        <authentication-provider>
            <user-service id="userService">
                <user name="admin" password="admin" authorities="ROLE_ADMIN" />
                <user name="user" password="user" authorities="ROLE_USER" />
            </user-service>
        </authentication-provider>
    </authentication-manager>
</beans:beans>